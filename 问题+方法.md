### 三、问题建模 (PROBLEM FORMULATION)

我们将 $N$ 个智能体 $\mathcal{I}=\{1,\dots,N\}$ 在对抗环境中的多周期价值采集问题建模为一个随机性多智能体马尔可夫决策过程 (MDP)，记为 $\mathcal{M} = \langle \mathcal{S}, \mathcal{A}, \mathcal{P}, \mathcal{R}, \gamma \rangle$。系统环境抽象为一个无向图 $G=(\mathcal{V},\mathcal{E})$，包含一个基地节点 $v_0$ 和一系列任务节点 $v \in \mathcal{V}\setminus\{v_0\}$。每个任务节点 $v$ 初始具有效用 $u_v \ge 0$ 和采集风险 $r_v \in [0,1]$。

**状态空间 (State Space)** $\mathcal{S}$。全局状态 $s \in \mathcal{S}$ 由环境状态 $s_{\text{env}}$ 和所有智能体的个体状态 $s_k$ 组成，即 $s = (s_{\text{env}}, s_1, \dots, s_N)$。环境状态 $s_{\text{env}} = \{ (u_v, r_v) \mid v \in \mathcal{V}\setminus\{v_0\} \}$ 描述了所有任务节点的当前效用与风险。个体状态 $s_k = (v_k, U_k^{\text{carried}}, \text{is\_alive})$ 包含了智能体 $k$ 的位置 $v_k$、当前携带效用 $U_k^{\text{carried}}$ 和生存状态 $\text{is\_alive} \in \{\text{True},\text{False}\}$。

**动作空间 (Action Space)** $\mathcal{A}$。联合动作 $a = (a_1, \dots, a_N) \in \mathcal{A}$ 是所有个体动作的笛卡尔积 $\mathcal{A} = \times_{k \in \mathcal{I}} \mathcal{A}_k$。若智能体失效 ($\text{is\_alive}_k=\text{False}$)，其唯一的动作是 $\mathcal{A}_k=\{\text{Stay\_Dead}\}$。一个存活的智能体 $k$ 在状态 $s$ 的动作空间 $\mathcal{A}_k(s)$ 包括：移动到邻近节点 ($\text{Move}(v_j)$, 其中 $v_j \in \text{Neighbors}(v_k)$)，在原地停留 ($\text{Skip}$)，以及在满足条件 ($v_k \neq v_0 \land u_{v_k}>0$) 时执行采集 ($\text{Collect}$)。

**状态转移与奖励 (Transitions and Rewards)** $\mathcal{P}$ 和 $\mathcal{R}$。系统动态由状态转移概率 $\mathcal{P}(s'|s,a)$ 定义。我们关注最大化团队总奖励 $R(s,a)=\sum_{k \in \mathcal{I}} R_k(s,a)$，其中 $R_k$ 是个体奖励。

- 若智能体 $k$ 执行 $\text{Move}(v_j)$，转移是确定性的 ($v_k' \leftarrow v_j$)。当 $v_j=v_0$ (返回基地) 时，智能体交付其携带的效用，产生一个即时奖励 $R_k = U_k^{\text{carried}}(t)$，同时其携带效用被清零 $U_k^{\text{carried}} \leftarrow 0$。所有其他移动动作的奖励 $R_k=0$。
- 若 $k$ 执行 $\text{Collect}$，转移是随机的。
  1. **成功** (概率 $1-r_{v_k}$): 智能体获得效用 $U_k^{\text{carried}} \leftarrow U_k^{\text{carried}} + u_{v_k}$，该节点资源被清空 $(u_{v_k}, r_{v_k}) \leftarrow (0,0)$。奖励 $R_k=0$。
  2. **失败** (概率 $r_{v_k}$): 智能体永久失效 $\text{is\_alive}_k \leftarrow \text{False}$，其携带效用全部丢失 $U_k^{\text{carried}} \leftarrow 0$，并产生一个惩罚 $R_k = -C$ (其中 $C \ge 0$)。节点资源保持不变。
- 对于 $\text{Skip}$ 或 $\text{Stay\_Dead}$ 动作，状态不发生改变且 $R_k=0$。

目标与终止条件 (Objective and Termination)。一个回合 (Episode) 在所有任务效用被清零 ($u_v=0, \forall v \neq v_0$) 或所有智能体均失效时终止。我们的目标是寻找一个最优联合策略 $\pi^*$，以最大化从初始状态 $s_0$ 开始的累计期望折扣奖励 $J(\pi)$：
$$
\max_{\pi} \ J(\pi) = \mathbb{E}_{\pi}\left[\sum_{t=0}^{T} \gamma^t R_t \mid s_0 \right]
$$

其中 $\gamma \in (0,1]$ 是折扣因子，T 是终止时间步。

### 四、方法论：VQ-HC-SAC 框架

为解决“问题建模”中定义的高维、稀疏奖励MDP，我们设计了一个新颖的、端到端的强化学习框架，称为**向量量化分层聚类软演员-评论家 (VQ-HC-SAC)**。

本框架的核心思想是将 $N$ 个智能体动态聚类为 $M$ 个（$M \ll N$）共享策略的角色，并通过分层的 SAC 模块进行训练。我们将该框架的架构**自顶向下**分解为三个核心组件进行阐述：

#### A. 分层 Actor-Critic 执行模块

为解决 $N$ 智能体带来的可扩展性挑战，本框架的顶层执行单元不再是 $N$ 个独立的网络，而是 $M$ 套可共享的角色专用网络。当智能体 $k$ 在 $t$ 时刻被分配了角色 $c_k$ 后（$c_k \in \{1,\dots,M\}$），它将执行该角色独有的一套模块，包括:

+ **$M$ 个角色策略 (Actors) $\{\pi_c\}_{c=1}^M$**: 智能体 $k$ 据此执行动作 $a_k(t) \sim \pi_{c_k}(\cdot | s_k(t))$。

+ **$M$ 个角色评论家 (Critics) $\{Q_c\}_{c=1}^M$**: 为保持输入维度与 $N$ 解耦，评论家 $Q_c$ 的输入包含个体信息 $(s_k, a_k)$、全局状态 $s_{\text{env}}$，以及 $M$ 个角色的**聚合状态表征** $\bar{s}_{\mathcal{G}_j} = \text{Pool}(\{s_i \mid c_i(t) = j\})$。

+ **$M$ 个角色温度 (Alphas) $\{\alpha_c\}_{c=1}^M$**: 每个角色 $c$ 拥有独立的可学习温度 $\alpha_c$ 和目标熵 $\mathcal{H}_{\text{target}, c}$。这实现了“角色一致性探索”，例如允许“开拓者”角色（高风险）拥有高熵，而“运输者”角色保持低熵。

#### B. VQ-Role 动态角色分配模块 ($\Psi$)

上述分层执行的前提是，智能体 $k$ 必须知道它在 $t$ 时刻属于哪个角色 $c_k$。我们设计了 VQ-Role 模块 ($\Psi$) 来端到端地学习这种分配，取代了“硬编码”规则。该模块由两部分组成：

+ **状态编码器 $E_\phi$**: 一个参数为 $\phi$ 的神经网络，它将智能体的个体状态 $s_k$ 压缩为一个低维嵌入 $z_k = E_\phi(s_k)$，其中 $z_k \in \mathbb{R}^D$。

+ **角色编码本 $\mathcal{E}$**: 一个可学习的矩阵 $\mathcal{E} \in \mathbb{R}^{M \times D}$，它存储了 $M$ 个“角色原型”向量 $e_j$，代表 $M$ 个角色的“锚点”。

在 $t$ 时刻，智能体 $k$ 的角色 $c_k$ 是通过**向量量化 (VQ)**（即最近邻查找）确定的。该计算过程通过 $\arg\min$ 函数实现，即寻找编码本 $\mathcal{E}$ 中与 $z_k$ 欧氏距离最近的原型 $e_j$：
$$
c_k(t) = \Psi(s_k(t)) = \arg\min_{j \in \{1,\dots,M\}} \| E_\phi(s_k(t)) - e_j \|^2
$$

#### C.信用分配与端到端训练

最后，我们必须解决如何训练这个由 A 和 B 组成的复杂架构。我们面临两大挑战：一是“问题建模”中定义的稀疏奖励，二是 B 模块中 $\arg\min$ 带来的不可微问题。

**1. 解决稀疏奖励：基于势能的奖励塑形 (PBRS)**

在我们的MDP中，奖励 $R_k$ 仅在 `Deliver` 时（$R_k = U_k^{\text{carried}}$）获得，而在关键的 `Collect` 时 $R_k=0$，这导致了严重的时间信用分配问题。

我们采用 PBRS 来稠密化奖励信号。我们定义一个**势能函数 $\Phi(s_k)$**，其值等于智能体当前携带的效用：
$$
\Phi(s_k) = U_k^{\text{carried}}
$$
所有 A 模块中的 Actor 和 Critic 均使用塑形后的奖励 $R'_k$ 进行训练。$R'_k$ 的计算方式如下：
$$
R'_k(s_k, a_k, s_k') = R_k + \gamma \Phi(s_k') - \Phi(s_k)
$$
其中 $R_k$ 是原始奖励，$\gamma$ 是折扣因子，$\Phi(s_k)$ 和 $\Phi(s_k')$ 分别是当前状态和下一状态的势能。这种塑形将信用从 `Deliver`（$R'_k=0$）完美转移到了 `Collect`（$R'_k \approx +\gamma u_v$）。

**2. 解决不可微分配：VQ-VAE 训练机制**

$\arg\min$ 操作是不可微的，它阻止了梯度从 A 模块（Actor-Critic 损失）回传到 B 模块的编码器 $E_\phi$。

我们采用 VQ-VAE 中的**直通估计器 (Straight-Through Estimator, STE)**。在反向传播时，我们将 Actor-Critic 损失对所选角色原型 $e_{c_k}$ 的梯度 $\nabla_{e_{c_k}}$，**直接复制**到 $E_\phi$ 的输出 $z_k$ 上，即 $\nabla_{z_k} \leftarrow \nabla_{e_{c_k}}$。这使得 $E_\phi$ 能够学习到“有利可图”的状态表征。

为稳定训练，我们还引入了两个辅助损失：

- 编码本损失 ($L_{\text{codebook}}$)：将角色原型 $e_{c_k}$ 拉向 $E_\phi$ 的输出 $z_k$。
  $$
  L_{\text{codebook}} = \mathbb{E} \left[ \| \text{sg}(E_\phi(s_k)) - e_{c_k} \|^2 \right]
  $$
  其中 $\text{sg}(\cdot)$ 是停止梯度 (stop-gradient) 算子，确保 $E_\phi$ 不受此损失的影响。

- 承诺损失 ($L_{\text{commit}}$)：将 $E_\phi$ 的输出 $z_k$ 拉向它所选择的原型 $e_{c_k}$。
  $$
  L_{\text{commit}} = \beta \cdot \mathbb{E} \left[ \| E_\phi(s_k) - \text{sg}(e_{c_k}) \|^2 \right]
  $$
  其中 $\beta$ 是一个控制承诺强度的超参数。

**总损失**:
$$
L_{\text{Total}} = \sum_c (L(Q_c) + J(\pi_c) + J(\alpha_c)) + L_{\text{codebook}} + L_{\text{commit}}
$$
